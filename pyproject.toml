[project]
name = "job-assistant"
version = "0.1.0"
description = "AI-powered job search and resume-tailoring assistant"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.setuptools.packages.find]
include = ["app*"]
